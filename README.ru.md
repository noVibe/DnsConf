🌐 Languages: [**English**](README.md) | [**Русский**](README.ru.md)

# Конфигуратор блокировок и перенаправлений DNS

**Автоматизирует настройку правил блокировки и перенаправления DNS для Cloudflare и NextDNS.**

**Готов к запуску через GitHub Actions или локально на Node.js 22+.**
[Видео с настройкой](#видео-пошаговой-настройки-redirect-для-nextdns)

## Быстрый старт

### Локальное использование

```bash
# Клонировать и установить
git clone <repository-url>
cd dns-block-and-redirect-configurer
npm install

# Настроить окружение
cp .env.example .env
# Отредактировать .env с вашими данными

# Запустить
npm run build
npm start
```

### GitHub Actions

Автоматический запуск ежедневно в 01:30 UTC. См. [инструкцию по настройке](#настройка-github-actions).

---

## Содержание

- [Cloudflare vs NextDNS](#cloudflare-vs-nextdns)
- [Настройка учетных данных](#настройка-учетных-данных)
- [Конфигурация](#конфигурация)
- [Источники данных](#источники-данных)
- [Поведение скрипта](#поведение-скрипта)
- [GitHub Actions](#настройка-github-actions)
- [Локальная разработка](#локальная-разработка)

---

## Cloudflare vs NextDNS

Оба провайдера имеют бесплатные тарифы с определёнными ограничениями:

### Ограничения Cloudflare
- 100 000 DNS-запросов в день
- IPv4 DNS-запросы ограничены одним IP (DoH, DoT, IPv6 без ограничений)

### Ограничения NextDNS
- 300 000 DNS-запросов в месяц (достаточно для личного использования)
- API ограничен 60 запросами в минуту (медленное выполнение)

---

## Настройка учетных данных

### Cloudflare

1. **Создать аккаунт Zero Trust:**
   - Зарегистрируйтесь в Cloudflare, перейдите во вкладку _Zero Trust_
   - Выберите бесплатный тариф
   - Пропустите платёжные данные (_Cancel and exit_)

2. **Создать API токен:** https://dash.cloudflare.com/profile/api-tokens
   
   Необходимые разрешения:
   - `Account.Zero Trust : Edit`
   - `Account.Account Firewall Access Rules : Edit`
   
   Сохраните токен в переменную окружения `AUTH_SECRET`

3. **Получить Account ID:** https://dash.cloudflare.com/?to=/:account/workers
   
   Сохраните в переменную окружения `CLIENT_ID`

### NextDNS

1. **Сгенерировать API ключ:** https://my.nextdns.io/account
   
   Сохраните в переменную окружения `AUTH_SECRET`

2. **Получить ID конфигурации:**
   - Нажмите на логотип NextDNS
   - Скопируйте ID из секции _Endpoints_
   
   Сохраните в переменную окружения `CLIENT_ID`

---

## Конфигурация

Создайте файл `.env` или установите переменные окружения:

```bash
# Обязательно
DNS=CLOUDFLARE                    # или NEXTDNS
CLIENT_ID=your_account_id
AUTH_SECRET=your_api_token

# Опционально
BLOCK=https://example.com/hosts   # URL через запятую
REDIRECT=https://example.com/hosts # URL через запятую
```

---

## Источники данных

Каждый источник должен быть URL на файл в формате hosts.

Пример: `https://raw.githubusercontent.com/Internet-Helper/GeoHideDNS/refs/heads/main/hosts/hosts`

Несколько источников: `https://first.com/hosts,https://second.com/hosts`

### Правила блокировки (`BLOCK`)

Парсит файлы hosts, оставляя только строки с `0.0.0.0` или `127.0.0.1`:

```
0.0.0.0 domain.to.block      → блокирует domain.to.block
127.0.0.1 another.to.block   → блокирует another.to.block
1.2.3.4 domain.to.redirect   → игнорируется
```

### Правила перенаправления (`REDIRECT`)

Парсит файлы hosts, оставляя только перенаправления с кастомными IP:

```
1.2.3.4 domain.to.redirect   → перенаправляет на 1.2.3.4
0.0.0.0 domain.to.block      → игнорируется
127.0.0.1 another.to.block   → игнорируется
```

**Примечание:** Приоритет перенаправлений следует порядку источников. Первое совпадение выигрывает.

### Рекомендации для провайдеров

- **Cloudflare:** Можно использовать один источник для `BLOCK` и `REDIRECT`
- **NextDNS:** Используйте только `REDIRECT`, блокировки выберите вручную во вкладке _Privacy_

---

## Поведение скрипта

### Cloudflare

- **Всегда удаляет старые данные** перед применением новых правил
- Определяет старые данные по:
  - Префикс списка: `Blocked websites by script` / `Override websites by script`
  - Префикс правила: `Rules set by script`
  - Session ID в поле description

Для очистки всех настроек Cloudflare: запустите без значений `BLOCK` или `REDIRECT`.

### NextDNS

**Для `REDIRECT`:**
- Обновляет существующие домены если IP изменился
- Добавляет новые домены
- Сохраняет остальные настройки

**Для `BLOCK`:**
- Добавляет новые домены
- Сохраняет существующие настройки

**Полная очистка:** Только когда оба `BLOCK` и `REDIRECT` пустые.

---

## Настройка GitHub Actions

### Видео: [REDIRECT для NextDNS](https://www.youtube.com/watch?v=vbAXM_xAL5I)

### Шаги

1. **Сделать Fork репозитория**

2. **Создать окружение:**
   - Перейдите в _Settings_ → _Environments_
   - Создайте окружение: `DNS`

3. **Добавить секреты:**
   - `AUTH_SECRET` - API токен/ключ
   - `CLIENT_ID` - ID аккаунта/конфигурации

4. **Добавить переменные:**
   - `DNS` - имя провайдера
   - `BLOCK` - источники блокировок (опционально)
   - `REDIRECT` - источники перенаправлений (опционально)

### Расписание

- Запускается ежедневно в **01:30 UTC** (04:30 МСК)
- Измените расписание в `.github/workflows/github_action.yml`
- Ручной запуск через вкладку _Actions_ → _Run workflow_

---

## Локальная разработка

### Требования

- Node.js 22+
- npm

### Установка

```bash
npm install
cp .env.example .env
# Отредактировать .env с вашими данными
```

### Команды

```bash
npm run build       # Скомпилировать TypeScript
npm start           # Запустить приложение
npm run typecheck   # Только проверка типов
```

---

## Технологии

- **Runtime:** Node.js 22+
- **Язык:** TypeScript 5.x
- **HTTP клиент:** node-fetch
- **CI/CD:** GitHub Actions

---

## Лицензия

ISC
